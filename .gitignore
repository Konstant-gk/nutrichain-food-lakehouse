# Explanation files
/docs/explanation

# Secrets
.env
dbt/dbt_profiles.yml

# Python cache and compiled files
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.coverage
htmlcov/

# Local Databricks job-parameter notes 
databricks/JOB_PARAMETER_SETUP.md

# Local temporary data (too large for Git)
/tmp/
*.json

# Airflow logs and local database
airflow/logs
airflow/airflow.db
airflow/*.pid

# Virtual environment
venv/
.venv/
env/

# Editor and OS files
.DS_Store
.idea/
.vscode/
*.swp
dbt/profiles.yml
dbt/.user.yml
dbt/logs/
dbt/target/
# Local query/debug artifacts only — do not use bare *.sql (would ignore dbt/models).
logs/*.log
logs/*.sql
dbt/logs/*.log
dbt/logs/*.sql

# Accidental nested checkout (duplicate repo folder under project root)
/nutrichain-food-lakehouse/
