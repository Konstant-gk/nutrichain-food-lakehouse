# Open Food Facts — no API key required (fully public)
OPENFOOD_MAX_PAGES=100

# Databricks credentials
DATABRICKS_HOST=replace
DATABRICKS_HTTP_PATH=replace
DATABRICKS_TOKEN=replace

# UC Volume path for landing JSON files
DATABRICKS_VOLUME_PATH=/Volumes/nutrichain_lakehouse/bronze/raw_json_landing

# File upload controls
DATABRICKS_MAX_FILE_MB=10
DATABRICKS_UPLOAD_TIMEOUT_SECONDS=60
DATABRICKS_UPLOAD_MAX_RETRIES=3
DATABRICKS_UPLOAD_BACKOFF_SECONDS=1.5

# Databricks catalog and schema names
DATABRICKS_CATALOG=nutrichain_lakehouse
DATABRICKS_BRONZE_SCHEMA=bronze
DATABRICKS_SILVER_SCHEMA=silver
DATABRICKS_GOLD_SCHEMA=gold
DATABRICKS_BRONZE_TABLE=bronze_openfood_products_raw
DATABRICKS_SILVER_TABLE=silver_openfood_products
DATABRICKS_GOLD_FACT_TABLE=fact_product_nutrition
DATABRICKS_GOLD_DIM_PRODUCT=dim_product
DATABRICKS_GOLD_DIM_BRAND=dim_brand
DATABRICKS_GOLD_DIM_CATEGORY=dim_category
DATABRICKS_GOLD_DIM_COUNTRY=dim_country
DATABRICKS_GOLD_DIM_NUTRISCORE=dim_nutriscore

# Postgres (for Airflow metadata database)
POSTGRES_USER=replace
POSTGRES_PASSWORD=replace
POSTGRES_DB=replace

# Airflow admin credentials
AIRFLOW_ADMIN_USER=replace
AIRFLOW_ADMIN_PASSWORD=replace
AIRFLOW_ADMIN_EMAIL=replace@example.com

# Databricks Job IDs (set after creating jobs in Databricks UI)
DATABRICKS_BRONZE_JOB_ID=replace
DATABRICKS_SILVER_JOB_ID=replace
DATABRICKS_GOLD_JOB_ID=replace